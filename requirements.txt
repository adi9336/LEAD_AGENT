# ── Core web / API ─────────────────────────────────────────────
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
pydantic-settings>=2.2

# ── Persistence (SQLAlchemy works for both SQLite and Postgres) ─
sqlalchemy>=2.0

# ── Outbound HTTP for live integrations (monday / WhatsApp) ─────
httpx>=0.27

# ── LLM (LAZY-imported: only required for live scoring) ─────────
# In mock mode (default) the rules engine is used instead, so the
# test-suite and local runs need NONE of these installed.
langchain>=0.2
langchain-openai>=0.1
langsmith>=0.1

# ── Observability (LAZY-imported: only needed when LANGSMITH_TRACING=true) ──
# scan). The hygiene logic itself is a plain function and is fully
# unit-tested without Redis/Celery.
celery>=5.3
redis>=5.0

# ── Dev / tests ────────────────────────────────────────────────
pytest>=8.0
